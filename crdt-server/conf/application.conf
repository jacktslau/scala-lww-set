play.modules.enabled += "crdt.modules.RedisModule"
play.modules.enabled += "crdt.modules.ServiceModule"

play.filters.disabled += play.filters.csrf.CSRFFilter  // no cookie authentication, no need CSRF protection
play.filters.disabled += play.filters.hosts.AllowedHostsFilter

redis {
  host = "localhost"
  port = 6379
}